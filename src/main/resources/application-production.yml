spring:
  datasource:
    url: jdbc:h2:mem:crypto
    username: Mikita
    password: breakfast
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
logging:
  level:
    root: INFO
scheduler:
  cron: "0 * * * * *"